lint:
  stage: tests
  extends: .node-template
  script:
    - pnpm install
    - pnpm lint

typecheck:
  stage: tests
  extends: .node-template
  script:
    - pnpm install
    - pnpm typecheck

spelling:
  stage: tests
  extends: .node-template
  script:
    - pnpm install
    - pnpm spelling

e2e:
  stage: tests
  extends: .node-template
  script:
    - pnpm install --frozen-lockfile
    - pnpm playwright:install
    - pnpm test:e2e
  artifacts:
    when: always
    paths:
      - playwright-report/
    expire_in: 30 days
